---

- hosts: all
  become: true
  tasks:

  - name: update repo index
    apt:
      update_cache: yes 

  - name: install apache2 and php packages
    apt:
      name:
        - apache2
        - libapache2-mod-php
      state: latest


